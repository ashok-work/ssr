import { PlacesAutoCompleteDirective } from './places-auto-complete.directive';

describe('PlacesAutoCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new PlacesAutoCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
